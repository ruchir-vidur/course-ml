# Runtime dependencies for the course RAG API, kept lean for small hosts (Render).
# Embeddings run through FastEmbed (ONNX runtime) — NOT torch/sentence-transformers
# — which drops ~2GB of deps and the associated RAM. Same model (BAAI/bge-small-en-v1.5).
# Pinned to a set verified to resolve together on Python 3.12.

# --- llama-index stack (interlocked; all require core<0.15,>=0.13) ---
llama-index-core==0.14.23
llama-index-embeddings-fastembed==0.6.0
llama-index-llms-groq==0.5.0
llama-index-llms-openai==0.6.26
llama-index-llms-openai-like==0.5.3
llama-index-vector-stores-qdrant==0.10.2

# --- lightweight ONNX embedding backend ---
fastembed==0.8.0

# --- vector DB (cloud), data parsing, and the API server ---
qdrant-client==1.18.0
webvtt-py==0.5.1
pymongo==4.10.1
python-dotenv==1.2.2
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.13.4
